//This program is written to crash if the password is typed incorrectly three times 
var password = prompt("Set your password")
let pin = "Mof3oluwa";

for(i = 0;i<=0;i++){
  prompt("Confirm your password")
}


let attempts = 3;
var login = prompt("Login with password")
for(i = 0;i<=1;i++){
  alert("Wrong Password!")
  prompt("Login with password")
  function stop(){
  clearTimeout(animate);
}
}
